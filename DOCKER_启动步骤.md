# 🐳 Docker 一键启动 - 3分钟快速指南

## 📋 检查清单

在开始之前，确保已安装：

```bash
# 检查Docker
docker --version          # ✓ 需要 Docker 20.10+

# 检查Docker Compose
docker-compose --version  # ✓ 需要 v2.0+

# 检查Java
java -version            # ✓ 需要 JDK 11+

# 检查Maven
mvn -version            # ✓ 需要 Maven 3.6+

# 检查Node.js
node -v                 # ✓ 需要 Node 16+
```

## 🚀 启动步骤（3个命令）

```bash
# 步骤1: 进入项目目录
cd linkfox-ai-design

# 步骤2: 赋予执行权限（首次需要）
chmod +x start.sh stop.sh

# 步骤3: 一键启动
./start.sh
```

## ⏱️ 启动时间

- 首次启动：约3-5分钟（需要下载Docker镜像和安装npm包）
- 后续启动：约1-2分钟

## ✅ 验证启动成功

启动成功后，您会看到：

```
======================================
✓ 所有服务启动成功！
======================================

服务地址：
  前端: http://localhost:3000
  后端: http://localhost:8080/api
  MySQL: localhost:3306
  Redis: localhost:6379

测试账号：
  用户名: admin 或 testuser
  密码: 123456
```

## 🌐 访问应用

1. **打开浏览器** → http://localhost:3000
2. **登录系统**
   - 用户名：`admin`
   - 密码：`123456`
3. **开始使用** 🎉

## 🛑 停止服务

```bash
./stop.sh
```

## 📊 查看状态

```bash
# 查看Docker容器状态
docker-compose ps

# 查看MySQL日志
docker-compose logs -f mysql

# 查看后端日志
tail -f logs/backend.log

# 查看前端日志  
tail -f logs/frontend.log
```

## 🔧 管理数据库

### 连接数据库

```bash
# 方式1: 使用Docker命令
docker-compose exec mysql mysql -uroot -plinkfox123456 linkfox_db

# 方式2: 使用本地MySQL客户端
mysql -h localhost -P 3306 -u root -plinkfox123456 linkfox_db
```

### 数据库信息

| 项目 | 值 |
|------|-----|
| 主机 | localhost |
| 端口 | 3306 |
| 数据库 | linkfox_db |
| 用户名 | root |
| 密码 | linkfox123456 |

### 重置数据库

```bash
# 停止并删除数据
docker-compose down -v

# 重新启动（会自动初始化）
docker-compose up -d mysql
```

## ❓ 常见问题

### 1. 端口被占用

```bash
# 检查端口占用
lsof -i :3306  # MySQL
lsof -i :6379  # Redis
lsof -i :8080  # 后端
lsof -i :3000  # 前端

# 解决方案：停止占用端口的程序，或修改端口
```

### 2. Docker服务未启动

```bash
# macOS: 打开Docker Desktop
open -a Docker

# Linux: 启动Docker服务
sudo systemctl start docker
```

### 3. 权限不足

```bash
# 如果看到 "Permission denied" 错误
chmod +x start.sh stop.sh

# 如果Docker需要sudo，将用户加入docker组
sudo usermod -aG docker $USER
# 然后重新登录
```

### 4. MySQL启动慢

首次启动MySQL需要初始化，可能需要30-60秒。请耐心等待。

```bash
# 查看MySQL启动进度
docker-compose logs -f mysql
```

### 5. 前端依赖安装慢

国内用户可以配置npm镜像：

```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 然后重新启动
./stop.sh
./start.sh
```

## 🔄 常用操作

### 重启所有服务

```bash
./stop.sh
./start.sh
```

### 仅重启数据库

```bash
docker-compose restart mysql redis
```

### 清理并重新启动

```bash
# 完全清理（删除数据）
docker-compose down -v

# 重新启动
./start.sh
```

### 查看服务资源占用

```bash
# 查看Docker容器资源
docker stats linkfox-mysql linkfox-redis
```

## 📁 项目目录说明

```
linkfox-ai-design/
├── docker-compose.yml    # Docker配置文件
├── start.sh             # 启动脚本
├── stop.sh              # 停止脚本
├── logs/                # 日志目录
│   ├── backend.log      # 后端日志
│   ├── frontend.log     # 前端日志
│   ├── backend.pid      # 后端进程ID
│   └── frontend.pid     # 前端进程ID
└── docker/
    └── mysql/
        └── my.cnf       # MySQL配置
```

## 🎯 下一步

启动成功后，您可以：

1. **体验功能** - 上传图片，测试AI处理
2. **查看代码** - 了解项目结构和实现
3. **开始开发** - 基于现有代码进行二次开发
4. **阅读文档** - 查看API文档和开发规范

## 📚 相关文档

- [README.md](README.md) - 完整项目说明
- [DOCKER_QUICKSTART.md](DOCKER_QUICKSTART.md) - Docker详细指南
- [QUICKSTART.md](QUICKSTART.md) - 传统方式快速启动
- [docs/API文档.md](docs/API文档.md) - API接口文档
- [docs/部署指南.md](docs/部署指南.md) - 生产环境部署

## 💡 提示

- ⚡ 首次启动较慢，后续启动会很快
- 💾 数据会自动保存，重启不会丢失
- 🔧 所有配置都可以自定义修改
- 📝 日志文件可以帮助排查问题

---

## 🆘 获取帮助

如果遇到问题：

1. **查看日志** - `logs/` 目录下的日志文件
2. **查看文档** - 本项目包含完整的文档
3. **提交Issue** - 在GitHub上提交问题

---

🎉 **祝您使用愉快！**

项目地址：`/tmp/linkfox-ai-design/`

